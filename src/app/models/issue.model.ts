export interface Issue {
		 issueNumber: number,
		 issueCategory: string,
		 issueDescription: string,
		 issueStartDate: Date,
		 issueEndDate: Date,
		 numVotes: number,
		 status: string,
		//public comments: TicketComment[]) 
}

